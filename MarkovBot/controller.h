#pragma once

namespace MarkovBot
{
	/*
	 * Responsible for controlling the interactions between I/O and the Producer and Consumer classes.
	 */
	class controller
	{
	public:
		controller(void);
		~controller(void);
	};
}